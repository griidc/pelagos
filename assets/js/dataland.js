import Vue from "vue";

window.addEventListener("load", function(event) {

    // here is the Vue code
    new Vue({
        el: '#dataland',
        template: `<div class="bootstrap">
                    <h1>Dataland</h1>
                   </div>`
    });
});
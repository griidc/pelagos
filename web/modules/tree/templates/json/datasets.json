[
{% for dataset in datasets %}
    {
        "data": {
            "title": "{{dataset.title | raw}}",
            "attr": {
                "style": "color:{{tree.dataset_color}}"
            }
        }
        ,
        "attr": {
            "id": "datasets_ID_{{dataset.udi}}",
            "action": "{{tree.dataset_action | evaluate}}"
        }
    }
    {% if not loop.last %}
    ,
    {% endif %}
{% endfor %}
]

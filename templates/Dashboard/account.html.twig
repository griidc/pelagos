<div>
    <h1 class="text-3xl font-bold mb-6">Account Information</h1>

    <div class="pb-4">
        <span class="block font-bold text-lg">Username:</span>
        <span class="block">{{ person.account.userName }}</span>
    </div>

    <div class="pb-4">
        <span class="block font-bold text-lg">Email:</span>
        <span class="block">{{ person.emailAddress }}</span>
    </div>

    <div class="mt-4">
        <a href="{{ path('pelagos_app_ui_account_changepassword') }}" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
            Change Password
        </a>
    </div>
</div>

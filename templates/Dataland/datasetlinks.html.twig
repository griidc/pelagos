<h2>Dataset Links:</h2>
<div>
    {% for link in dataset.datasetSubmission.datasetLinks %}
        <p>
            <b>Name:</b>{{ link.name }}<br>
            <b>Description:</b>{{ link.description }}<br>
            <b>Function:</b>{{ link.functionCode }}<br>
            <b>URL:</b>
            <a href="{{ link.url }}" target="_blank">
                {{ link.url }}
            </a>
        </p>
    {% endfor %}
</div>
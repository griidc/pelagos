[
{% for letter in letters %}
    {
        "id": "{{letter}}",
        "text": "{{letter}}",
        "li_attr": {
            "style": "color:black"
        },
        "children": true,
        {% if tree.expand_to_depth > 0 %}
            "state": "open"
        {% else %}
            "state": "closed"
        {% endif %}
    }
    {% if not loop.last %}
    ,
    {% endif %}
{% endfor %}
]

[
{% for letter in letters %}
    {
        "text": "{{letter}}",
        "li_attr": {
            "style": "color:black"
        },
        "a_attr": {
            "id": "{{letter}}"
        },
        {% if tree.expand_to_depth > 0 %}
            "state": "open"
        {% else %}
            "state": "closed"
        {% endif %}
    }
    {% if not loop.last %}
    ,
    {% endif %}
{% endfor %}
]

[
{% for dataset in datasets %}
    {
        "data": {
            "title": "{{dataset.getTitle | raw}}",
            "attr": {
                "style": "color:{{tree.dataset_color}}"
            }
        }
        ,
        "attr": {
            "id": "datasets_ID_{{dataset.getUdi}}",
            "action": "{{tree.dataset_action | evaluate}}"
        }
    }
    {% if not loop.last %}
    ,
    {% endif %}
{% endfor %}
]
